# Deprecated Mobile Stub

The legacy JavaScript mobile UI shell has been removed.

- Active mobile client: `rpm_flutter_client/` (Flutter, App A/B modes)
- Shared JS runtime used by demo backend/tests: `shared/runtime/`

This directory is retained only to make the migration explicit for existing links.
